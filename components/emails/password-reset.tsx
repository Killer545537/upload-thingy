import { Html } from '@react-email/components';

const PasswordResetEmail = ({
    name,
    resetUrl,
}: {
    name: string;
    resetUrl: string;
}) => <Html></Html>;

export default PasswordResetEmail;
